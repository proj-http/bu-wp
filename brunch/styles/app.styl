@import 'nib'

*, *::before, *::after {
  box-sizing border-box
}

html, body {
  margin 0
  padding 0
}

h1, h2, h3, h4, h5, h6 {
  margin 0
  padding 0
}

#map-canvas {
  width 100%
  height 100%
  position absolute
  top 0
  left 0
}
